import { SHARED_PACKAGE_VERSION } from "@claudeops/shared";

// Scaffolding only (page1). Real server/controller wiring lands in page4
// (see .claude/plans and .claude/MASTER_PLAN.md).
console.log(`ClaudeOps controller scaffold. shared package v${SHARED_PACKAGE_VERSION}`);
